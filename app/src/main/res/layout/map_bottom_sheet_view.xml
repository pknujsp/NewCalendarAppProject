<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/white"
    android:orientation="vertical"
    app:behavior_hideable="true"
    app:layout_behavior="com.google.android.material.bottomsheet.BottomSheetBehavior">

    <LinearLayout
        android:id="@+id/bottom_sheet_item_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:visibility="visible">

        <include
            android:id="@+id/place_view"
            layout="@layout/map_bottom_sheet_place"
            android:visibility="gone" />

        <include
            android:id="@+id/address_view"
            layout="@layout/map_bottom_sheet_address"
            android:visibility="gone" />

        <include
            android:id="@+id/map_bottom_sheet_toolbar"
            layout="@layout/map_bottom_sheet_toolbar" />

    </LinearLayout>

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/map_bottom_sheet_fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

</LinearLayout>