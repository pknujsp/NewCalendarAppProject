<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/menu_item_today"
        android:checkable="true"
        android:icon="@drawable/today_icon"
        android:title="today"
        app:showAsAction="always" />
    <item
        android:id="@+id/menu_item_refresh"
        android:checkable="true"
        android:icon="@drawable/refresh_icon"
        android:title="refresh"
        app:showAsAction="always" />

    <item
        android:id="@+id/monthFragment"
        android:title="@string/calendar_type_month" />
        <item
            android:id="@+id/weekFragment"
            android:title="@string/calendar_type_week" />
        <item
            android:id="@+id/dayFragment"
            android:title="@string/calendar_type_day" />
</menu>